name: test
channels:
  - conda-forge
dependencies:

  - python
  - pip

    # --- Core ---
  - pydantic
    # """ RDKit """
  - rdkit

    # --- Testing ---
  - pytest
  - pytest-cov
  - codecov

    # --- Developer ---
  - isort
  - black
  - flake8
